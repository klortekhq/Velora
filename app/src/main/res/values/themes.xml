<resources>

    <!-- Main theme without splash screen - used for all activities except MainActivity -->
    <style name="Theme.Velora" parent="@style/Theme.Leanback">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <!-- Splash theme - only used for MainActivity (app launch) -->
    <style name="Theme.Velora.Splash" parent="@style/Theme.Velora">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>

